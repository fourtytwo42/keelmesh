apiVersion: v1
kind: ConfigMap
metadata: {name: keelmesh-platform}
data:
  KEELMESH_KAFKA_BROKERS: bootstrap.MSK_CLUSTER.REGION.amazonaws.com:9098
  KEELMESH_DATABASE_URL: postgres://keelmesh@RDS_PROXY.proxy-CLUSTER.REGION.rds.amazonaws.com:5432/keelmesh?sslmode=verify-full
